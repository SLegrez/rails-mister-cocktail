<div class="container">
<!--   <h1><%#= @cocktail.name %></h1>
  <p>Ingredients</p>
  <%#= cl_image_tag @cocktail.photo.key, height: 200, width: 300, crop: :fill %>
  <button type="button" class="btn btn-warning"><%#= link_to 'Back to menu', cocktails_path %></button>-->
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @cocktail.photo.key, height: 200, width: 300, crop: :fill %>')">
    <h2><%= @cocktail.name %></h2>
    <p>Ingredients</p>
    <% @cocktail.doses.each do |dose| %>
      <%= dose.description %>
      <%= link_to "Delete dose", dose_path(dose), method: :delete, data: { confirm: "Are you sure?"} %>
    <% end %>
  </div>

  <%= link_to "Add Dose", new_cocktail_dose_path(@cocktail) %>
  <button type="button" class="btn btn-warning" style="margin-left: 10px; border-radius: 4px;"><%= link_to 'Back to menu', cocktails_path %></button>
</div>
